name: Pages

on:
  push:
    branches: [main]
    paths:
      - "index.html"
      - "download.html"
      - "robots.txt"
      - "sitemap.xml"
      - "docs/assets/**"
      - "Pawse/Resources/Assets.xcassets/AppIcon.appiconset/**"
      - "Pawse/Resources/Assets.xcassets/PawseBanner.imageset/**"
      - "Pawse/Resources/Assets.xcassets/PawseLogo.imageset/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Assemble static site
        run: |
          mkdir -p _site/docs/assets
          mkdir -p _site/Pawse/Resources/Assets.xcassets/AppIcon.appiconset
          mkdir -p _site/Pawse/Resources/Assets.xcassets/PawseBanner.imageset
          mkdir -p _site/Pawse/Resources/Assets.xcassets/PawseLogo.imageset
          cp index.html download.html robots.txt sitemap.xml _site/
          cp docs/assets/pawse-hero.png _site/docs/assets/
          cp docs/assets/pawse-menu.png _site/docs/assets/
          cp docs/assets/pawse-break.jpg _site/docs/assets/
          cp docs/assets/pawse-settings.png _site/docs/assets/
          cp Pawse/Resources/Assets.xcassets/AppIcon.appiconset/icon_32x32@2x.png _site/Pawse/Resources/Assets.xcassets/AppIcon.appiconset/
          cp Pawse/Resources/Assets.xcassets/PawseBanner.imageset/PawseBanner.png _site/Pawse/Resources/Assets.xcassets/PawseBanner.imageset/
          cp Pawse/Resources/Assets.xcassets/PawseLogo.imageset/PawseLogo.png _site/Pawse/Resources/Assets.xcassets/PawseLogo.imageset/
          touch _site/.nojekyll

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
